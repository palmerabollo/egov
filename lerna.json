{
  "version": "0.0.0",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["*.spec.*", "*.md"],
      "message": "chore(release): lerna publish"
    }
  }
}

service: egov-adapter-traffic-radars

provider:
  name: aws
  runtime: python3.7
  memorySize: 3000
  timeout: 60
  region: eu-west-1
  stage: dev
  versionFunctions: false
  environment:
    maintainer: egov

functions:
  adapt:
    handler: handler.adapt
    events:
      # The AWS API Gateway has a 30-seconds timeout
      - http:
          path: "{proxy+}"
          method: get

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: non-linux
